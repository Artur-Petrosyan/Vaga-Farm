import Section from "../Section/Section"

const Section3 = ({ h1, p, button, img, eclipse }) => {
    return (
        <Section
            h1={h1}
            button={button}
            p={p}
            img={img}
            eclipse={eclipse}
        />
    )
}

export default Section3;